# Veritas Installation files
.DS_Store
*.log
*.swp
*~

# IDE files
.vscode/
.idea/
*.code-workspace

# Temporary files
tmp/
temp/

# Node modules (if added later)
node_modules/
package-lock.json

# Research radar local-only data
radar/data/jobs.json
radar/data/local-state.json
radar/data/profile*.json
# The authored profile document — this is Christian, in a public repo.
radar/data/profile*.md
radar/data/resumes/
radar/data/route-cache.json
radar/data/resume*.txt
radar/data/resume*.md
radar/data/dol-raw/
radar/data/enrichment-cache/

# Build artifacts (if build process added)
dist/
build/

# OS files
Thumbs.db
ehthumbs.db
Desktop.ini

# Archive files
*.zip
*.tar.gz

# Development notes
notes.md
TODO.md

# Python scout
scout/.venv/
__pycache__/
*.pyc
radar/data/aggregated/
.codex/

# Local digest secrets (never commit)
radar/scripts/.digest.env

# local data snapshots must never enter history
radar/data/jobs.json.stale*

# Judged matches + stated preferences: derived from private resume data
radar/data/match-cache.json
radar/data/preferences.json

# API keys — this repo is public. Never let an env file near a commit.
.env
.env.*
!.env.example

# Sharded crawl output — merged into ats-discovery.json, never committed raw
scout/.shards/
